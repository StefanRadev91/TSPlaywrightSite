import POMContent from './POMContent';

export const metadata = {
  title: 'Page Object Model',
  description: 'Master the Page Object Model pattern for test automation. Learn enterprise-level architecture, base page patterns, and component objects.',
};

export default function POMPage() {
  return <POMContent />;
}
